version: 2
customFilters:
  filter: ".vulnerabilities |= map(if .cvssScore >= 10 then . else empty end)"
  pass: "[.vulnerabilities[] | select(.cvssScore >= 10)] | length"
  msg: "Vulnerabilities with CVSS Score of 9+ found"
